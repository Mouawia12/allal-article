/**
 * Storage provider contracts will live here.
 *
 * <p>Keep provider-specific code behind interfaces so Cloudflare R2 can be
 * swapped later without changing domain modules.</p>
 */
package com.allalarticle.backend.attachments.storage;
